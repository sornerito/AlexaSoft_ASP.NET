@model IEnumerable<AlexaSoft_ASP.NET.Models.Ventasdetallesservicio>

@{
    ViewData["Title"] = "Detalles Servicios";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <div class="row">
        <div class="col-3"></div>
        <div class="col-6">
            <h1 class="text-center">Detalles Servicios</h1>
        </div>
        <div class="col-3"></div>
    </div> <!--Fin Row-->
</div>

<p>
    <a asp-action="Create" class="btn btn-lg fondoVino text-light">Crear Detalle</a>
</p
<div class="container">
<table class="table fs-6 table-bordered table-responsive" style="overflow:auto;">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subtotal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPaqueteNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdVentaNavigation)
            </th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subtotal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPaqueteNavigation.IdPaquete)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdVentaNavigation.IdVenta)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdVentaDetalleServicio" class="btn btn-l fondoOro text-light">Editar</a>
                <!-- <a asp-action="Details" asp-route-id="@item.IdVentaDetalleServicio">Details</a> -->
                <a asp-action="Delete" asp-route-id="@item.IdVentaDetalleServicio" class="btn btn-l fondoVino text-light">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
