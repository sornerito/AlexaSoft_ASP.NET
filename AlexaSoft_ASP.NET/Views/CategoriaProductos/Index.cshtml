@model IEnumerable<AlexaSoft_ASP.NET.Models.CategoriaProducto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table fs-5 table-bordered table-responsive " style="overflow:auto;">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCategoriaProducto">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCategoriaProducto">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCategoriaProducto">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
