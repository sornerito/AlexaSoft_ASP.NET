@model IEnumerable<AlexaSoft_ASP.NET.Models.CategoriaProducto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container">
    <div class="row">
        <div class="col-3"></div>
        <div class="col-6">
            <h1 class="text-center">Categoria Productos</h1>
        </div>
        <div class="col-3"></div>
    </div>
</div>

<p>
    <a asp-action="Create" class="btn btn-lg fondoVino text-light">Crear Categoria</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.IdCategoriaProducto">Editar</a> |
                <a class="btn btn-success" asp-action="Details" asp-route-id="@item.IdCategoriaProducto">Detalles</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.IdCategoriaProducto">Eliminar</a>
            </td>
        </tr>
}   
    </tbody>
</table>
